__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/
.DS_Store

# data & training artifacts
data/
checkpoints/
*.bin
*.pt
wandb/

# internal scratchpad — never commit (see ~/.claude/CLAUDE.md)
docs/superpowers/

# local-only literature review (kept on disk, not in the repo)
sota-review/

# private experiment log — kept on disk, not in the public repo
EXPERIMENTS.md

# X-post draft — scratch, not part of the repo
x_post/
